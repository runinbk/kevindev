"use client";

export const LangToggle = () => {
  return (
    <button
      className="px-4 py-2 border border-border rounded-full text-[11px] font-bold tracking-widest uppercase text-muted-foreground hover:bg-foreground hover:text-background transition-colors"
    >
      ES / EN
    </button>
  );
};
